# Copy to mydevsite repo as .github/workflows/deploy.yml when ready.
# Production example — wiltsu.dev (deploy_path must match bedrock_deploy_path in site_bedrock.yml).

name: Deploy to production

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    uses: Wiltzsu/platform/.github/workflows/reusable-deploy-bedrock.yml@master
    with:
      deploy_path: /var/www/wiltzsu
      theme_path: web/app/themes/mydevsite
      php_version: "8.4"
      node_version: "20"
    secrets:
      SSH_KEY: ${{ secrets.SSH_KEY }}
      HOST: ${{ secrets.HOST }}
      USER: ${{ secrets.USER }}
