---
# Superseded by site-bedrock.yml for wiltsu.dev / Bedrock sites.
# See docs/drafts/bedrock-wiltsu-deploy-plan.md

- name: Configure nginx site
  hosts: all
  gather_facts: false
  tasks:
    - name: Use site-bedrock playbook instead
      ansible.builtin.fail:
        msg: >-
          Use playbooks/site-bedrock.yml for Bedrock WordPress sites.
          This placeholder is kept for future generic nginx-only runs.
